# To Do

Remember that:
1) Song can be loaded with unedited metadata.
2) User updates with correct title, artists and year metadata.
3) System should recognize that and merge playCount and totalPlayTime for both IDs.

This allows for users to merge data for songs that were removed and added back, as well as updating metadata for specific songs without losing data.